services:
  contract-intelligence:
    build: .
    container_name: contract-intelligence-app
    ports:
      - "8501:8501"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    stdin_open: true
    tty: true